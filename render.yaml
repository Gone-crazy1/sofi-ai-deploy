services:
  - type: web
    name: sofi-ai
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn main:app"
    plan: free
    envVars:
      - key: FLASK_ENV
        value: production
    ports:
      - 10000
